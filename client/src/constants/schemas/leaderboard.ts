export interface LeaderboardSchema {
  name: string;
  accuracy: number;
  wpm: number;
}

export interface Leaderboard {
  id: string;
  name: string;
  accuracy: number;
  wpm: number;
}
